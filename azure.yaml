# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: custom-chat-copilot-sk-base
services:
    web:
        project: ./app/backend
        host: containerapp
        language: dotnet
        docker:
            path: ../Dockerfile
            context: ../